#include<stdio.h>
struct st{
int roll;
char *name;
float marks;

};

float calculate(float x)
{
    if()
}


int main(void)
{
    float calculate();
    struct st student[50];
    int n,i;
    printf("How many student:");
    scanf("%d",&n);
    for( i=1;i<=n;i++){
        printf("%d.",i);
        scanf(" %10s%10d%10f",&student[i].name,&student[i].roll,&student[i].marks);
    }

}
